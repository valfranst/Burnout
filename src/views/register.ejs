<div class="auth-wrapper">
  <div class="card auth-card">
    <h2>Criar conta</h2>
    <p class="text-muted mb-2" style="font-size:.9rem;">
      Informe seu nome, e-mail e senha para receber recomendações personalizadas de bem-estar e garantir acesso seguro ao painel da equipe.
      Os dados ajudam o modelo a reconhecer seu perfil e evitar falsos positivos de burnout.
    </p>
    <div id="alert-box"></div>

    <a href="/auth/google" class="btn btn-google" style="width:100%; justify-content:center;">
      <svg width="18" height="18" viewBox="0 0 48 48"><path fill="#EA4335" d="M24 9.5c3.54 0 6.71 1.22 9.21 3.6l6.85-6.85C35.9 2.38 30.47 0 24 0 14.62 0 6.51 5.38 2.56 13.22l7.98 6.19C12.43 13.72 17.74 9.5 24 9.5z"/><path fill="#4285F4" d="M46.98 24.55c0-1.57-.15-3.09-.38-4.55H24v9.02h12.94c-.58 2.96-2.26 5.48-4.78 7.18l7.73 6c4.51-4.18 7.09-10.36 7.09-17.65z"/><path fill="#FBBC05" d="M10.53 28.59c-.48-1.45-.76-2.99-.76-4.59s.27-3.14.76-4.59l-7.98-6.19C.92 16.46 0 20.12 0 24c0 3.88.92 7.54 2.56 10.78l7.97-6.19z"/><path fill="#34A853" d="M24 48c6.48 0 11.93-2.13 15.89-5.81l-7.73-6c-2.15 1.45-4.92 2.3-8.16 2.3-6.26 0-11.57-4.22-13.47-9.91l-7.98 6.19C6.51 42.62 14.62 48 24 48z"/></svg>
      Cadastrar com Google
    </a>

    <div class="divider">ou</div>

    <form id="register-form">
      <div class="form-group">
        <label for="name">Nome</label>
        <input type="text" id="name" name="name" placeholder="Seu nome">
        <small class="text-muted" style="font-size:.75rem;">Use pelo menos 2 caracteres e evite siglas para facilitar a personalização.</small>
      </div>
      <div class="form-group">
        <label for="email">E-mail</label>
        <input type="email" id="email" name="email" required placeholder="seu@email.com">
        <small class="text-muted" style="font-size:.75rem;">Informe o e-mail corporativo para garantir acesso ao dashboard.</small>
      </div>
      <div class="form-group">
        <label for="password">Senha</label>
        <input type="password" id="password" name="password" required minlength="6" placeholder="Mínimo 6 caracteres">
        <small class="text-muted" style="font-size:.75rem;">Mínimo de 6 caracteres; use letras e números para fortalecer o acesso.</small>
      </div>
      <button type="submit" class="btn btn-primary" style="width:100%; justify-content:center;">Criar conta</button>
    </form>

    <p class="text-center text-muted mt-2" style="font-size:.85rem;">
      Já tem conta? <a href="/login">Entrar</a>
    </p>
  </div>
</div>

<script>
  document.getElementById('register-form').addEventListener('submit', async (e) => {
    e.preventDefault();
    const form = e.target;
    const box = document.getElementById('alert-box');
    App.hideAlert(box);
    const nameValue = form.name.value.trim();
    const emailValue = form.email.value.trim();
    const passwordValue = form.password.value;

    if (nameValue.length < 2) {
      App.showAlert(box, 'Informe um nome válido com ao menos 2 caracteres.');
      return;
    }
    if (!/\S+@\S+\.\S+/.test(emailValue)) {
      App.showAlert(box, 'Informe um e-mail válido com domínio e aroba.');
      return;
    }
    if (passwordValue.length < 6) {
      App.showAlert(box, 'A senha deve ter no mínimo 6 caracteres.');
      return;
    }
    try {
      await App.api('/auth/register', {
        method: 'POST',
        body: JSON.stringify({
          name: nameValue,
          email: emailValue,
          password: passwordValue,
        }),
      });
      App.showAlert(box, 'Conta criada! Redirecionando...', 'success');
      setTimeout(() => window.location.href = '/login', 1500);
    } catch (err) {
      App.showAlert(box, err.error || 'Erro ao cadastrar.');
    }
  });
</script>
